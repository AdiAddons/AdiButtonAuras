language: erlang

env:
  - LUA="lua5.1" LUAC="luac5.1"

install:
  - sudo apt-get install $LUA

script:
  - tests/lualint.sh
  - tests/cs.sh

notifications:
  email:
    on_failure: always
    on_success: change
